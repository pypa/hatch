version: '3'

services:
  devpi:
    container_name: hatch-devpi
    build:
      context: devpi
    ports:
    - "3141:3141"
    environment:
      - DEVPI_INDEX_NAME
      - DEVPI_USERNAME
      - DEVPI_PASSWORD
